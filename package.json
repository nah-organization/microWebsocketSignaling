{
    "scripts": {
        "build": "tsc",
        "start": "node --enable-source-maps dist/main.js",
        "dockerup": "docker compose up --build -d",
        "dockerdown": "docker compose down",
        "dev": "npm run dockerdown; npm run dockerup; say 'ドッカーが起動しました'"
    },
    "devDependencies": {
        "@types/node": "^18.11.10",
        "@types/ws": "^8.5.4",
        "typescript": "^4.9.3"
    },
    "dependencies": {
        "@8128-33550336/random": "^0.4.3",
        "ws": "^8.12.0"
    }
}
